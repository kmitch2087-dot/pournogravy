import { Navigate } from "react-router-dom";

// Collections have been consolidated — everything lives in /shop now.
const Collections = () => <Navigate to="/shop" replace />;

export default Collections;
